[project]
name = "comfyui_arkennemasis"
description = "arkennemasis — ComfyUI nodes for many AI use cases: Replicate LLM + image gen nodes (more providers coming) plus utility nodes (System Instructions)."
version = "1.1.0"
license = { text = "MIT" }
dependencies = ["replicate"]

[project.urls]
Repository = "https://github.com/Hishamahmer/comfyui-arkennemasis"

[tool.comfy]
PublisherId = "Hishamahmer"
DisplayName = "arkennemasis Nodes"
Repository = "https://github.com/Hishamahmer/comfyui-arkennemasis"
