[project]
name = "comfyui_arkennemesis"
description = "ArkenNemesis — ComfyUI nodes for many AI use cases: Replicate LLM + image gen nodes (more providers coming) plus utility nodes (System Instructions, Color Picker, Palette Analyzer)."
version = "1.1.0"
license = { text = "MIT" }
dependencies = ["replicate"]

[project.urls]
Repository = "https://github.com/Hishamahmer/comfyui-arkennemesis"

[tool.comfy]
PublisherId = "Hishamahmer"
DisplayName = "ArkenNemesis Nodes"
Repository = "https://github.com/Hishamahmer/comfyui-arkennemesis"
